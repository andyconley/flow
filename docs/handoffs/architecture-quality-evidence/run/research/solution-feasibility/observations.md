# Feasibility observations — not pilot acceptance
2026-09-10. Public GitHub release latest and tag API verified v0.28.0 at recorded commit. Downloaded archive into isolated /private/tmp path; no Git metadata in archive.
Python 3.14.6 macOS arm64. 64 CodexCollectorTests + ClaudeCollectorTests passed in 0.246s (test runner measurement). Under coverage:64 tests passed in 0.817s. These are existing synthetic/in-memory tests, not live usage harvesting. Timing is compatibility evidence only, not pilot reviewer benefit.
Tach 0.35.0 maps flat CLI source roots without rewriting imports. Check/report initially fails because cli/setup.py is mistaken for packaging metadata. An analyzer-only cli/pyproject.toml in the disposable copy fixes resolution; original .py bytes unchanged. Must keep analyzer overlay separate and manifest-bound.
Probe policy denies every module edge to expose machine-readable line/source diagnostics. Its violations are deliberate instrument output, NOT findings of real Flow architectural defects and NOT an approved baseline/policy. tach report cli/harvest.py --dependencies emits source line evidence; --raw loses that detail.
Radon 6.0.1: read_new_lines complexity 5, lines28–58. Coverage 7.14.0 emits native per-function JSON:18/18 statements and6/6 branches covered. CRAP using declared statement coverage=5. Mutation evidence remains unverified. Full coverage does not establish assertion effectiveness.
Source identity, environment and raw reports retained alongside. Tools installed only in /private/tmp/flow-architecture-tools. No viewer or implementation built.
