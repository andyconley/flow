# Role-method differentiation evidence

Each role receives a frozen primary case that requires its new method and a
counter-case that punishes over-application. Treatment and unchanged-body
control use identical client settings; treatment must satisfy every distinctive
rubric item the control misses. Ties fail.

The original three-role comparison remains below as historical evidence.
`repair-2/` is the separately frozen two-role replacement attempt. Its eight
records completed successfully, but both strict role gates failed; it therefore
does not make the five-role expansion eligible for merge.
