# Planning business analysis

The named artifacts are contracts and lifecycle mechanics. The durable operator job is to turn approved intent into one bounded, explainably delegated, independently verified real change without losing authority to MAF, a provider, or a runtime session.

Required main path:

`approved intent -> atomic start-plan -> sealed charter and lead generation -> Magentic choice with rationale -> Flow grant -> bounded producer change -> Ollama verification -> sealed receipt`

Edge cases include transition replay, stale generation, timeout without takeover, explicit resume/supersede, unresolved action blocking, verifier failure, v6 inspection-only behavior, and all invalid inputs failing before send.

Advisory expertise request `a51dc38f-62bf-432f-965b-eb67bd5b45ae` applied: the plan preserves both the requested system artifacts and the underlying verified-delivery outcome. Post-receipt digest: `d4da08cbb81d782727d9bc8803ced60b5997000a57ec3fa180592eccf2e11701`.
