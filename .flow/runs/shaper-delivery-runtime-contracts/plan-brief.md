# Chunk 1 planning brief

Plan the approved first vertical slice so another engineer can implement it without chat history. The slice must add canonical Shaper Contract and Delivery Charter records, atomic `start-plan` ownership handoff, one generation-fenced Delivery Lead, a new projection into the existing Flow-gated Magentic runtime, inspect-only v6 compatibility, and an operator-facing CLI inspection command. Magentic may choose an approved Claude or Codex producer and must record why; implementation chooses the smallest suitable real Flow change. Deterministic tests gate normal implementation, and one live producer-plus-Ollama-verifier job is required at handback. Ordinary Chat MCP, later cross-runtime adapters, amendments, nested providers, and broad operational hardening remain deferred. Planning only; do not implement.
