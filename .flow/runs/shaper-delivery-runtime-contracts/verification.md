# Definition verification plan

Before approval, verify that requirements and acceptance criteria cover the approved direction, all material claims have provenance/disposition, the orchestration manifest validates, and the proposal remains in `defining`. After approval, use `flow-solution` for schema, migration, adapter, and amendment design.
