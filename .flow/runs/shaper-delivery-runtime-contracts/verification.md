# Definition verification plan

Before solution approval, verify that the selected architecture preserves Flow authority, keeps MAF and provider state behind adapters, defines one fenced Delivery Lead, preserves v6 inspection without execution eligibility, and breaks work into independently plan-ready chunks. Confirm all material requirement clarifications are reconciled without rewriting approved definition history. Validate the orchestration manifest and require explicit engineer approval before `approve-solution`.
