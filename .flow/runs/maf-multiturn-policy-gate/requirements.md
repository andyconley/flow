# Requirements: multi-turn Magentic Flow controls

Approved continuation of the prior reviewed Magentic interruption spike. Local-only research; no paid calls or production dependency.

1. A Flow-owned builder accepts only specialist specifications and constructs guarded participants; raw Agent/Executor injection is rejected.
2. Two legitimate Magentic requests for the same specialist receive distinct IDs bound to run, charter, and instance; replaying a pre-dispatch checkpoint retains the ID and does not redispatch.
3. Actual Magentic replanning crosses the Flow gate. Two replans are allowed and a third denied before more provider work.
4. Capture executable evidence and an independent review. Distinguish prototype scope from global enforcement and MAF adoption.
