# Validation plan

Run isolated Python unit/integration tests in the disposable MAF environment. Assert distinct and replayed IDs, two successful dispatches, raw injection rejection, and replan denial. Inspect SQLite ledger and checkpoint records. Run syntax/diff checks. No paid provider calls.
