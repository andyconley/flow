# Bounded MAF Delivery Lead spike brief

Use the approved requirements and acceptance criteria in this run. Keep all worker work in a disposable repository. Flow owns dispatch authorization, spend limits, and receipt evidence. Stop a provider if cost cannot be bounded. Preserve the prior narrow spike and record each claim as observed, inferred, recommended, or unverified.

Evidence inventory: `requirements.md`, `acceptance-criteria.md`, `research/decision.md`, `research/maf_delivery_probe.py`, `research/maf-delivery-result.json`, `scaffolds/default/agents/`, `cli/runstate.py`, and `cli/orchestration.py`.
