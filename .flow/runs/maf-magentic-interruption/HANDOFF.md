# Handback: guarded Magentic dispatch and interrupted-call recovery

**Result:** the bounded local research slice passed independent review. An actual Magentic participant request reached the Flow guard. A disallowed specialist was denied with zero dispatch rows. A permitted local Ollama specialist produced a streamed update, then its disposable process exited intentionally; Flow marked the dispatch `unknown`. A separate process replayed the same MAF checkpoint and reached terminal `manual_reconciliation_required` without another Flow-recorded dispatch.

Evidence: [validation results](validation-results.md), [receipt](research/receipt.json), [review](review.md), and [reconciliation](reconciliation.md). No paid provider was called. The disposable Git fixture remained clean.

**Adoption remains pending.** The fixed one-call request ID needs a durable MAF invocation identity for multi-turn work; production construction must make guarded participants mandatory; Magentic replans must be Flow-authorized. A commit-bound receipt and a defensible roughly 70% Delivery Lead reduction estimate are also outstanding. Keep the Flow-native engine freeze while these are decided.

No Flow production code, installed dependency, or provider configuration changed. Research artifacts are run-local and disposable state is under `/private/tmp`.
