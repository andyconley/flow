# Technical-writer recommendation: review-ready handback

## Recommended handback wording

Repair-2 implemented the approved replacement surfaces for product-manager and
quality-reviewer. The production changes are narrow: each repaired corpus has
one replacement entry, superseded entries and competency edges are removed,
the selected role-body instructions are present, and the lead-developer
short/full-plan change is preserved. The generated Claude and Codex surfaces
remain covered by the shared renderer and adapter checks.

The frozen behavioral gate did not pass. Product-manager treatment classified
the value shapes and produced the expected order, but did not perform the
required delay-cost-per-duration comparison; the unchanged control also
performed material parts of the method. Quality-reviewer treatment supplied
the alternative explanation and discriminating evidence, but declared the
Critical before completing that chain; the unchanged control also supplied the
same substantive reasoning. Both counter cases behaved safely. Under the
frozen stopping rule, both role gates are false, so the combined five-role
release remains blocked. The repair-2 evidence is retained as a failed,
separately frozen attempt and must not be selectively rerun or rescored.

## Evidence strength

- **Observed:** eight successful Claude response envelopes; parity across each
  treatment/control pair; criterion-level behavioral scores; 45 focused tests;
  the mutation check showing the selected product instruction is required;
  full test suite, whitespace, generated-adapter, runtime-smoke, and doctor
  checks against the final tree.
- **Read:** frozen requirements, acceptance criteria, manifest, prompts, body
  hashes, corpus identifiers, competency joins, raw response hashes, and the
  retained prior role-method evidence.
- **Asserted or follow-up:** any claim requiring a live client to load and
  exercise the updated agents remains a separate manual check; source
  verification and byte-for-byte preservation claims should retain their
  referenced paths and hashes rather than being described as runtime proof.

## Next decision

Route this handback to `flow-review` for formal confirmation of the failed
behavioral gate. The decision after review belongs in `flow-define`: either
design a more distinctive PM/QR method and freeze a new evidence envelope, or
accept that the unchanged role bodies already provide these methods and close
the replacement scope without claiming behavioral differentiation. Do not
merge or release the five-role expansion while either gate remains false.

## Canonical evidence paths

- Frozen behavioral envelope: `docs/evidence/role-method-differentiation/repair-2/`
- Criterion-level results: `docs/evidence/role-method-differentiation/repair-2/results.json`
- Behavioral score and failure rationale: `.flow/runs/role-method-differentiation/research/behavioral-scoring-repair.md`
- Current validation and handback: `.flow/runs/role-method-differentiation/validation-results.md` and `HANDOFF.md`
- Reconciled findings: `.flow/runs/role-method-differentiation/findings-reconciliation.md`
