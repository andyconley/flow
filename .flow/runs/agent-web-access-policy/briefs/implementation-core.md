# Brief: Core Implementation

## Evidence inventory

- `requirements.md`, `solution.md`, `plan.md`, and `validation-plan.md`
- `cli/sync.py`, `cli/render.py`, `cli/flowtoml.py`
- `scaffolds/default/flow.toml` and all registered agent sources

Implement the semantic resolver, pre-write integration, native mappings,
guidance, scaffold default, and source cleanup. Own only `cli/`, the scaffold
manifest, and the solution architect source.
