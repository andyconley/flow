# Verification: v8-live-validation

The definition is independently challenged by the product, requirements, and architecture reviewers. `adversarial-architecture` is the verifier, distinct from the producer (`definition-root`, `docs-editor`) and the evidence collector (`definition-root`). The live job's verifier is the Flow-evaluated Ollama `local-verifier`, distinct from the Claude producer.
