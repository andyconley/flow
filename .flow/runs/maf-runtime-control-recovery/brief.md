# Repair brief

Evidence inventory: this run's `requirements.md` and `acceptance-criteria.md`; prior run `maf-delivery-runtime-spike/requirements.md`, `validation-results.md`, `review.md`, `reconciliation.md`, and `research/restart_probe.py`; Flow's `scaffolds/default/agents/`, `cli/runstate.py`, `cli/orchestration.py`.

Use the installed disposable MAF environment in `/private/tmp/flow-maf-runtime-spike-20260919`. Use no paid provider. Record policy decisions before MAF dispatch, keep process recovery evidence bounded, and check the Git fixture independently. Return a pass/fail verdict for every acceptance criterion.
