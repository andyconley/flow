# Implementation handoff: first supervised MAF execution slice

- Approved by Andy on 2026-09-19. Planning run: `maf-adoption-design`. Use [plan](plan.md) and [validation plan](validation-plan.md) as the implementation contract; use [ADR 0011](../../../docs/adr/0011-supervise-maf-behind-flow-gateway.md) and [adoption design](../../../docs/maf-adoption-design.md) for boundaries.
- Start `flow-implement` only after this run reaches `plan_approved`. Preserve the existing `run.json` lifecycle projection and `events.jsonl` transition history. Use an implementation run if needed to keep production code changes separate from this design/planning run; link the approved plan rather than silently widening it.
- First delivery: `flow run execute-local <work-id> --assignment <id> --task-file <path>` against an existing `implementing` run, with one effective `test-engineer` definition and one local Ollama model. CLI spelling may follow established command conventions, but its preconditions and behavior are fixed in the plan.
- Flow parent owns contract validation, SQLite gate, one-use grant consumption, physical local adapter call, independent receipt and lifecycle. The optional supervised MAF child owns workflow planning/checkpoint mechanics and constructs only the Flow-supplied guarded executor; it proposes work through bounded versioned stdio and cannot self-grant or call the adapter directly.
- Build order: contracts/identity → ledger/policy → specialist resolver/gateway/local adapter → optional child/supervisor → CLI/receipt/docs → automated validation and real Ollama smoke. Keep MAF imports out of base CLI modules.
- Failure rule: no automatic retry. Before dispatch, seal a no-dispatch failure receipt; after uncertain physical dispatch, mark `unknown` and require reconciliation. Completion evidence does not advance C-Lite handback automatically.
- Defer Codex, Claude, paid routes, MCP, parallel workers, live replans, restart/resume, delegated Shaper expansion and commit-bound code handback. Stable IDs/checkpoint references must leave room for those slices.
- Role evidence: [business analysis](research/plan-requirements.md), [product scope](research/plan-priority.md), [architecture](research/plan-integration.md), [test strategy](research/plan-validation.md). The expert retrieval for analyst, architect and test engineer returned `no_match`; no advisory entries were attached.
