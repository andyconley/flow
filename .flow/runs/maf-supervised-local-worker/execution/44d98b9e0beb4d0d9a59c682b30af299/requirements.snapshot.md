# Requirements: supervised local MAF worker

Implement the approved first slice from `.flow/runs/maf-adoption-design/plan.md`, accepted by Andy on 2026-09-19. This implementation run is separate because the adoption-design run specified no production mutation.

Flow owns execution records, policy, grants, physical local adapter calls, and receipts. A supervised optional MAF child proposes one guarded `test-engineer` action through a versioned protocol. The CLI launches it only for an existing `implementing` run with a valid manifest and approved task artifact. One physical local Ollama smoke and truthful stub tests are required. No paid provider, Codex/Claude/MCP, automatic replay, Shaper expansion, or production release in this slice.
