# Expertise applicability contract fixtures

This directory is reserved for protocol-only fixture metadata used by Gate 0/1
tests. It contains no calibration or held-out task text, target serialization,
model output, score, or candidate result. Real campaign fixtures are authored
only after a feasible result and the required independent assignments.
